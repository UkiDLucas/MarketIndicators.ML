# Functional Requirements: US_GDP_Q

1. Download/copy raw CSV for `US_GDP_Q`.
2. Persist raw output to `data_ingestion/OUTPUT/raw/`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/`.
4. Use local snapshot fallback when remote fetch fails and snapshot exists.
