# Functional Requirements: VUG

1. Fetch raw historical series for `VUG`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/VUG.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/VUG.json`.
4. Use local snapshot fallback when available.
