# Functional Requirements: ROBO

1. Fetch raw historical series for `ROBO`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/ROBO.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/ROBO.json`.
4. Use local snapshot fallback when available.
