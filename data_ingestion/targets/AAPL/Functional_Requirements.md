# Functional Requirements: AAPL

1. Fetch raw historical series for `AAPL`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/AAPL.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/AAPL.json`.
4. Use local snapshot fallback when available.
