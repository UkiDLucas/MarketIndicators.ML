# Functional Requirements: DIA

1. Fetch raw historical series for `DIA`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/DIA.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/DIA.json`.
4. Use local snapshot fallback when available.
