# Functional Requirements: TSLA

1. Fetch raw historical series for `TSLA`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/TSLA.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/TSLA.json`.
4. Use local snapshot fallback when available.
