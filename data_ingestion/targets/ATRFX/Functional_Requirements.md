# Functional Requirements: ATRFX

1. Fetch raw historical series for `ATRFX`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/ATRFX.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/ATRFX.json`.
4. Use local snapshot fallback when available.
