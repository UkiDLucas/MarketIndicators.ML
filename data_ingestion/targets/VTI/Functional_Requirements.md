# Functional Requirements: VTI

1. Fetch raw historical series for `VTI`.
2. Save raw CSV to `data_ingestion/OUTPUT/raw/VTI.csv`.
3. Persist metadata JSON to `data_ingestion/OUTPUT/metadata/VTI.json`.
4. Use local snapshot fallback when available.
